{
  "name": "base",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "concurrently -n \"SER,CLI\" -c \"bgGreen,bgBlue\" \"yarn server\" \"yarn client\"",
    "bootstrap": "lerna bootstrap",
    "client": "yarn workspace client run start",
    "server": "yarn workspace server run watch",
    "app": "yarn workspace app run watch",
    "lint": "tslint -c tslint.json 'packages/**/src/**/*.ts*'",
    "fix": "yarn lint -- --fix"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^12.11.1",
    "concurrently": "^5.0.0",
    "lerna": "^3.18.1",
    "nodemon": "^1.19.4",
    "ts-node": "^8.4.1",
    "tslint": "^5.20.0",
    "typescript": "^3.6.4"
  }
}
